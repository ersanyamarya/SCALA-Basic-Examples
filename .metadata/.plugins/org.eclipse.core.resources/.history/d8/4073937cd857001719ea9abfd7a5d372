package com.sanyam.test.practice

object arraySwap {
  def main(args: Array[String]): Unit = {
    val arr = Array(1, 2, 3, 4, 5)
    for (a <- 0 to arr.length; b <- 0 until arr.length) {
      print("a is: |" + a + "|")
      println(" b is: |" + b + "|")
    }
  }
}