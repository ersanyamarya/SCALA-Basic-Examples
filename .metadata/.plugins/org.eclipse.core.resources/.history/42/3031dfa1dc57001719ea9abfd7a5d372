package com.sanyam.test.practice

object arrayArrange {
  def main(args: Array[String]): Unit = {
    var arr = Array(1, 2, -5, -9, 5)
    for (i <- 0 to arr.length){
      if((arr(i)+(arr(i)*-1).toInt)==0){
        println(arr(i))
      }
    }
  }
}